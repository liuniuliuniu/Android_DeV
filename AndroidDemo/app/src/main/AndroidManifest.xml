<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liushaohua02.androiddemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--android:name=".ResApplication" 指定初始化文件-->
    <application
        android:name=".ResApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.view.activity.SplashActivity"
            android:theme="@style/AppTheme_NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.view.activity.RegisterActivity" />
        <activity android:name=".ui.view.activity.LoginActivity" />
        <activity android:name=".ui.view.activity.OrderActivity"></activity>
    </application>

</manifest>